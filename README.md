# EmelyanovTGBot - Railway Production Version

Telegram WebApp с интерактивной историей, развернутый на Railway с PostgreSQL базой данных.

## 🚀 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone <your-repo-url>
cd EmelyanovTGBot
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

#### Для локальной разработки:
Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

Заполните переменные в `.env`:
```env
BOT_TOKEN=your_bot_token_here
WEBAPP_URL=https://your-railway-app-name.up.railway.app
```

#### Для Railway (Production):
1. Перейдите в Railway Dashboard
2. Выберите ваш проект
3. Перейдите в раздел "Variables"
4. Добавьте следующие переменные:
   - `BOT_TOKEN` = ваш токен Telegram бота
   - `WEBAPP_URL` = URL вашего Railway приложения
   - `DATABASE_URL` = автоматически предоставляется Railway

### 4. Запуск

#### Локально:
```bash
python bot.py
```

#### На Railway:
Автоматический деплой при push в main ветку.

## 🔧 Структура проекта

- `bot.py` - Telegram бот
- `railway-server-db.py` - Flask сервер с PostgreSQL
- `webapp/` - Frontend (25 страниц истории)
- `requirements.txt` - Python зависимости
- `Procfile` - Railway конфигурация

## 🔒 Безопасность

- `.env` файл исключен из Git (в `.gitignore`)
- Переменные окружения хранятся в Railway Variables
- Токены не попадают в публичный репозиторий

## 📱 Функциональность

- Интерактивная история через Telegram WebApp
- Сохранение прогресса в PostgreSQL
- Система уведомлений владельцев
- Адаптивный дизайн для мобильных устройств 